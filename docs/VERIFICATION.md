# Verification record — 0.3.0

Verification on Linux / Python 3.12, 2026-09-22.

| Check | Actual result |
|---|---|
| `python3 -m unittest discover -s tests -v` | 43 tests passed |
| `bash run.sh test` after setup | 43 tests passed in the dedicated virtual environment |
| `bash setup.sh` | Created/reused venv, installed hash-locked dependencies, preflight ready |
| `bash setup.sh --offline` | Repeat setup succeeded without downloads |
| `node --check secaudit/static/app.js` | Passed |
| Synthetic source assessment | 5 expected candidate findings; dependency database explicitly missing |
| Dashboard API | Authentication, Host protection, CSRF rejection, scan submission, completion and PDF download passed |
| PDF export | Both reports generated; embedded font rendering inspected |
| Scanner adapters | JSON fixtures, malformed output, output bounds, timeout and blocked-isolation tests passed |
| Advisory snapshots | Exact-version matching, integrity rejection, freshness policy and CLI path passed |
| Offline core bundle | Install/run tests with kernel network denial passed |
| Full wheel bundle | Installed in a fresh venv with seccomp network denial; installed CLI produced 5 findings and PDFs |
| Windows | Launch/path tests only; no actual Windows/WSL execution here |
| External scanner binaries | Not available; Bubblewrap namespace execution denied by host |
| Real AI providers | Not tested; local and API protocol stubs passed |
| Browser UI end-to-end | Not completed: Chromium download unavailable in this environment; API and JS syntax checks passed |

The first setup attempt encountered an existing corrupt pip bytecode cache in the
workspace venv. Removing generated bytecode and rerunning setup succeeded. No source
or user data was removed. This is recorded rather than hiding the failed attempt.

Tests never scan public targets. No real provider credentials or target evidence
are committed. Linux verification does not establish production readiness or complete
security coverage. See CAPABILITIES.md for unsupported parts of the original brief.
